# which backend the app talks to. one knob drives both the dev proxy and the
# native http base. defaults to the hosted beta when unset
VITE_API_HOST=https://pumg.fyi

# Anime playback is configured on the backend adapter, never in this public
# Vite environment. See adapter/README.md.
